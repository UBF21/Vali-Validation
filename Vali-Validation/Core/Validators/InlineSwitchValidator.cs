namespace Vali_Validation.Core.Validators;

/// <summary>
/// Internal concrete validator used to collect rules inside switch cases.
/// </summary>
internal sealed class InlineSwitchValidator<T> : AbstractValidator<T> where T : class { }
